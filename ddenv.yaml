# This file defines the dependencies so that ddenv
# (https://github.com/denisdefreyne/ddenv) can install and
# set them up automatically.

up:
  - homebrew: openjdk
  - homebrew: asciidoc
  - ruby
  - bundle
